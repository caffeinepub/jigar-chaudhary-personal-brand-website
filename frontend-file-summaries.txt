{
  "frontend/index.html": "HTML entry point with Google Fonts preconnect and imports for Bebas Neue and Inter fonts, plus updated page title.",
  "frontend/src/index.css": "Global CSS with OKLCH dark theme tokens, Google Fonts typography, smooth scroll, orange/gold gradient utilities, and new CSS custom properties for the register page color tokens (reg-dark, reg-navy, reg-card, reg-gold, reg-muted).",
  "frontend/tailwind.config.js": "Extended Tailwind config with brand color tokens, register page color tokens (reg-dark, reg-navy, reg-card, reg-gold, reg-muted mapped to CSS variables), Bebas Neue display font, Inter body font, orange/gold glow shadows, and custom animations.",
  "frontend/src/App.tsx": "Root app component with hash-based routing supporting three routes — main site, /#/admin (AdminPanel), and /#/register (RegisterPage) — each rendered exclusively without mixing sections.",
  "frontend/src/components/CounterAnimation.tsx": "Reusable animated counter component using the custom useInView hook and requestAnimationFrame with cubic ease-out to animate numbers from 0 to a target value on first viewport entry.",
  "frontend/src/components/Navigation.tsx": "Sticky navigation bar with all existing links plus a new gold-accented \"Live Session\" button (with animated pulse dot) in both desktop and mobile menus that navigates to /#/register, visually distinct from the orange \"Book Now\" CTA.",
  "frontend/src/components/Hero.tsx": "Full-screen hero section with two existing CTA buttons (\"Book Jigar Now\", \"Explore Programs\") plus a new gold-accented \"Live Session\" button with a pulse dot and Radio icon that links to /#/register; all three buttons wrap responsively on mobile.",
  "frontend/src/components/About.tsx": "About/Who is Jigar section updated to use ws9-about.dim_1200x800.jpeg (classroom selfie with students) as the profile image, preserving the orange/gold gradient border frame and all bio content.",
  "frontend/src/components/Programs.tsx": "Signature Programs section with 5 program cards using per-card useInView hooks for staggered scroll-triggered fade-in animations and CSS hover lift+glow effects — no framer-motion dependency.",
  "frontend/src/components/Corporate.tsx": "Corporate Workshops section with animated header, description card, workshop topic list with icons, CTA button, and a two-stat row (100% Custom, Pan-India Reach) — \"50+ Corporates\" stat removed.",
  "frontend/src/components/Testimonials.tsx": "Testimonials section with 4 cards showing only star ratings and quote text — name and institution/company attribution fields removed from both the data array and card rendering.",
  "frontend/src/components/MediaCertifications.tsx": "Event Gallery section with all 24 photos in a responsive grid — each image is now clickable and opens the Lightbox component for full-size viewing, with keyboard accessibility and a \"Click to view\" hint on hover.",
  "frontend/src/components/CTABanner.tsx": "CTA Banner section with the large \"TRANSFORM\" watermark text removed and replaced with a subtle dot-pattern background and soft glow orb, keeping the headline, body copy, and CTA buttons as the clear visual focus.",
  "frontend/src/components/Contact.tsx": "Contact/booking section with the form now wired to the backend via `useSubmitBooking` mutation — shows a loading spinner on the submit button, disables all fields during submission, displays an error message on failure, and resets the form with a success confirmation on completion.",
  "frontend/src/components/Footer.tsx": "Dark minimal footer with logo, quick links, and all social icons (Instagram, LinkedIn, YouTube) updated to Jigar's real profile URLs with target=\"_blank\" and rel=\"noopener noreferrer\", plus dynamic copyright year and caffeine.ai attribution.",
  "frontend/src/hooks/useInView.ts": "Custom React hook using the native IntersectionObserver API to detect viewport entry, with the return type corrected to `RefObject<T | null>` to match React 19's `useRef` signature.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; `useGetAllSubmissions` and `useGetAllRegistrations` now additionally check `!!identity` from `useInternetIdentity` so they only execute when the actor is genuinely authenticated (not anonymous), preventing unauthorized calls and permissions errors.",
  "frontend/src/components/AdminPanel.tsx": "Admin panel with three-step flow (II login → password gate → dashboard); now clears stale query cache before entering the panel step, shows an `ActorLoadingScreen` while the authenticated actor is initializing (`actorFetching`), and sets `isAuthenticated` only when the actor is fully ready — eliminating the race condition that caused the permissions error.",
  "frontend/src/components/Lightbox.tsx": "New reusable full-screen lightbox overlay component that accepts isOpen, imageSrc, imageAlt, and onClose props; supports closing via overlay click, Escape key, or close button, and locks body scroll while open.",
  "frontend/src/components/RegisterPage.tsx": "Full-page registration experience for the \"Pressure to Performance\" live session on 8th March 2026, with all session timing references updated from \"6:00 PM IST\" to \"10:00 AM – 12:00 PM IST\" and the SESSION_DATE constant corrected to 04:30 UTC (10:00 AM IST). The \"Duration\" detail card label is also updated to show \"10 AM – 12 PM IST\".",
  "frontend/src/hooks/useCountdown.ts": "Custom React hook that calculates and returns a live countdown (days, hours, minutes, seconds) to a target date, updating every second via setInterval, with an `isLive` flag when the target time is reached."
}